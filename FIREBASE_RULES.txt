// Firestore Rules
// Copy and paste this into your Firebase Console -> Firestore Database -> Rules

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow read/write access to all users under any conditions
      // WARNING: This is for development only. Secure your database before production.
      allow read, write: if true;
    }
  }
}

// Storage Rules
// Copy and paste this into your Firebase Console -> Storage -> Rules

rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow read/write access to all users under any conditions
      // WARNING: This is for development only. Secure your storage before production.
      allow read, write: if true;
    }
  }
}
